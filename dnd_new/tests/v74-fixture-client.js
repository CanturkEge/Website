/* Synthetic campaign bootstrap after the actual production script chain. */
(async()=>{const {data,error}=await db.rpc('fixture_boot');if(error)throw error;auth=data.auth;current=data.campaign;campaigns=[current];state=normalized({...blank(),...data.state});members=data.members;prEnsure();v31Baseline=JSON.parse(JSON.stringify(state));v31BaselineCampaign=current.id;document.body.classList.remove('landing-mode');document.body.insertAdjacentHTML('beforeend','<output id="fixture-result" aria-label="Test işlem sonucu" style="display:block;padding:8px;color:#e8d8b7;font:12px system-ui">İzole test hazır</output>');page='encounter';render();})();
